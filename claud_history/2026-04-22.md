# 2026-04-22

## 本次更改
- 将 `src/App.vue` 改为路由出口，作为页面承载入口。
- 在 `src/router/index.ts` 中新增 `/login` 路由，并将 `/` 重定向到 `/login`。
- 新建 `src/views/LoginView.vue`，实现 Vue 登录页面，包含用户名、密码输入框和登录按钮。
- 为登录页补充基础样式，使用卡片式布局和居中展示。
- 移除 `vite.config.ts` 中的 `vite-plugin-vue-devtools` 插件，关闭页面底部的 Vue 调试模块。
- 在 `src/App.vue` 中去掉页面默认外边距，并在 `src/views/LoginView.vue` 中限制溢出，修复页面边缘白边和滚动条问题。
- 在 `src/views/LoginView.vue` 中写入注释形式的后端登录接口示例，并使用写死账号 `admin / 123456` 完成登录跳转逻辑。
- 新建 `src/views/HomeView.vue`，新增登录成功后的简单 Hello World 页面，并在 `src/router/index.ts` 中接入 `/home` 路由。
