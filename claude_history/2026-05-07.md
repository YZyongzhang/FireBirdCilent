# 2026-05-07 更改记录

- 把后端地址提取到 `src/config.ts` 中，导出常量 `API_BASE`。
- 在 `src/views/LoginView.vue` 中使用 `API_BASE` 构建请求地址（`${API_BASE}/login`）。
- 更新范围仅限 `src` 目录内。
- 已执行 `git add .` 与 `git commit`（提交信息见 commit 记录）。
# 2026-05-07 变更记录

本次变更（补充/重应用）：

- 确认并在 `src/main.ts` 中注册 `src/plugins/http.ts` 插件，确保全局 `http` 实例可用。
- 将 `src/views/LoginView.vue` 中对 `axios` 的直接引用替换为全局 `http` 实例（使用相对路径 `/login`，依赖 `http` 的 `baseURL`）。

之前已完成（同日早些时候）：

- 新增 `src/plugins/http.ts`：创建并导出 `http`（axios 实例），并作为 Vue 插件安装。
- 新增 `src/types/http.d.ts`：为组件全局属性 `$http` 添加 TypeScript 声明。

说明：

- 本次变更目标为将项目中所有数据获取集中为统一的 `http`（axios 实例），便于统一拦截/错误处理与 baseURL 管理。当前已处理 `LoginView.vue`，并在 `main.ts` 中注册插件以供全局使用。
