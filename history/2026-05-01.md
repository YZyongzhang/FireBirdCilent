# 2026-05-01

## 本次更改
- 将首页、思考记录页、每日计划页的静态默认数据统一整理到 `src/dataset` 目录。
- 首页卡片数据从页面组件中抽离到 `src/dataset/home.ts`。
- 思考记录页默认分组与默认文档改为从 `src/dataset/thoughts.ts` 读取。
- 每日计划页默认任务模板和初始计划数据改为从 `src/dataset/dailyPlan.ts` 读取。
- 移除思考记录页基于 `localStorage` 的读取与持久化逻辑。
- 移除每日计划页基于 `localStorage` 的读取与持久化逻辑。
- 页面运行时编辑仍然保留，但刷新后将回到 dataset 中定义的初始数据。

## 涉及文件
- `src/dataset/home.ts`
- `src/dataset/thoughts.ts`
- `src/dataset/dailyPlan.ts`
- `src/views/HomeView.vue`
- `src/views/ThoughtsView.vue`
- `src/views/DailyPlanView.vue`
